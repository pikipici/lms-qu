# Fase 8 E2E Status — 2026-05-23

## Remote Deploy

- Target: `rdpkhorur:/home/ubuntu/lms`
- Deploy path: manual build to `backend/bin/lms-api` + `systemctl restart lms-api`
- `readyz`: PASS

## Static Export Routing Fix

Root cause found: `/login` was serving `index.html` because Next static export writes `login.html`, while Fiber static did not resolve extensionless `/login` to `login.html` before the SPA fallback.

Fix: `backend/cmd/server/main.go` now resolves extensionless routes to `<route>.html` before mounting static/fallback handlers.

Verification after deploy:

- `curl http://127.0.0.1:8200/login` contains `nama@sekolah.id`
- Playwright browser probe sees login body, not landing page
- Login page input count: `2`

## Playwright Rate-Limit Hardening

`frontend/playwright.config.ts` now defaults to:

- `fullyParallel: false`
- `workers: 1`

This prevents auth smokes from triggering global/login rate limits during MVP smoke runs. `E2E_WORKERS` can override for future CI tuning.

## Latest Remote Run

Command:

```bash
cd /home/ubuntu/lms/frontend
npm run typecheck
npx playwright test --list
E2E_BASE_URL=http://127.0.0.1:8200 npm run e2e
```

Results:

- Typecheck: PASS
- Test discovery: PASS, `12` tests in `4` files
- Full E2E: `4 passed`, `8 failed`

Passing:

- login smoke: validation errors
- login smoke: admin session redirects to admin dashboard
- login smoke: force temporary-password users to security page
- guru login: validation errors

Remaining failures are test-contract issues, not the original `/login` routing issue:

- Some core-flow specs expect UI controls/labels that do not match current pages (`Tambah pengguna`, dashboard heading text, menu roles).
- Some mocked-login specs do not mock follow-up protected API calls needed by destination dashboards.
- A few later auth specs still encounter login-form waits after previous failures; keep auth smoke as the go-live gate until the expanded specs are aligned.

## Current MVP Gate Recommendation

Use `login-smoke.spec.ts` as the remote go-live auth smoke for now, because it passes against deployed static export and validates the critical login redirects.

Next hardening step: refactor expanded specs into stable page-object helpers and mock each destination page's required API calls before enabling them in the go-live suite.
