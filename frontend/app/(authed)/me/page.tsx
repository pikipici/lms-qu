// Self pages — profile + change password. Real form lands in Fase 1; this is
// the route stub so static export has the path.
export default function MePage() {
  return (
    <main className="container py-16">
      <h1 className="text-2xl font-semibold">Profil</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Halaman profil aktif di Fase 1.
      </p>
    </main>
  );
}
